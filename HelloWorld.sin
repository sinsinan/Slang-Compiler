function NUMERIC factorial(NUMERIC a) {
    if(a==1) {
        while(a >= 0) {
        STRING a;
        a = "cds";
        PRINTLINE(a);
        return 1;
        }
    }
    return a * factorial(a-1);
 }

PRINTLINE(factorial(4));